# Te Waihora 农场管理系统（FMS）

> **一句话：** 一个帮农场主模拟「牛吃草、草再长、牛群搬家」的 Web 小系统。  
> **技术栈：** Python · Flask · MySQL · Bootstrap  
> **性质：** COMP636 课程项目（2024），2026 年本地整理完善

---

## 给别人介绍时，可以这样说

> 这是一个农场管理模拟器。农场里有几块围起来的牧场（paddock），每块地上长草，牛以「群」（mob）为单位活动。  
> 系统会算：今天草还剩多少、够不够吃、要不要把牛群换到别的牧场。  
> 你可以点「下一天」，看草被吃掉、又长出来，颜色变黄变红就是提醒「这块地草不够了」。

如果对方是 **非技术背景**（朋友、家人、非技术岗面试官），重点讲 **「模拟什么 → 能点什么 → 看到什么」**。  
如果对方是 **技术背景**，直接跳到本文 **「技术实现」** 一节。

---

## 这个项目解决什么问题？

在新西兰，很多农场要管理：

- 有多少头牛、分几群
- 每群现在在哪个牧场
- 牧草够不够吃、要不要转场

人工算很费脑子，尤其是要 **往前推演「再过几天草还够吗」**。

FMS 把这件事做成了一个 **可操作的网页**：

1. 看现状（牛在哪、草多少）
2. 做操作（搬家、改围场参数）
3. 推时间（模拟下一天，自动重算牧草）

它不是真实农场用的生产系统，而是一个 **教学 + 演示性质** 的管理模拟器——但完整走通了 **数据库 → 后端逻辑 → 网页交互** 这条链路。

---

## 三个核心概念（30 秒版）

| 概念 | 通俗理解 | 系统里 |
|------|----------|--------|
| **Paddock（围场）** | 一块有围栏的牧场 | 有面积、牧草量，最多住一群牛 |
| **Mob（牛群）** | 一群牛，整群行动 | 有名字，绑在一个 paddock 上 |
| **Stock（牲畜）** | 单头牛 | 有 ID、生日、体重，属于某个 mob |

还有一个重要设定：**模拟日期**。

- 顶栏的日期 **不是今天真实日期**，是模拟器里的「农场日历」
- 点 **Advance to next day** = 过一天，系统重新算牧草生长和被吃掉的部分

---

## 功能亮点（介绍用）

| 你能做什么 | 会看到什么 |
|------------|------------|
| 看 **Mobs** | 每群牛在哪个围场、多少头、平均多重 |
| 看 **Stock** | 按群列出每头牛的年龄、体重 |
| 看 **Paddocks** | 每块地剩多少草；**黄/红行** = 草偏少或过少 |
| **Move mob** | 把一群牛迁到空围场 |
| **Edit paddocks** | 改围场面积、草量；可新增围场 |
| **Advance to next day** | 日期 +1，牧草数据自动更新 |

**演示时推荐路径：** Home → Paddocks（看颜色）→ Advance 几天（看变化）→ Move mob（看牛群换地）

---

## 三分钟演示脚本

适合给别人现场展示或录屏：

1. **打开首页** — 说明这是 Te Waihora 农场模拟器  
2. **进 Paddocks** — 指一下表格：哪块地有牛、DM/ha 多少、有没有变黄变红  
3. **点 Advance to next day** 2～3 次 — 指日期变了、牧草数值跟着变  
4. **进 Stock** — 展开一个 mob，看每头牛的 ID 和年龄  
5. **Move mob** — 选一群牛换到空围场，回 Paddocks 确认已更新  

---

## 项目背景

- **课程：** COMP636（Web 应用开发），林肯大学  
- **原始要求：** Flask + MySQL + Bootstrap，实现农场 CRUD、牧草模拟、报告文档  
- **2026 整理：** 毕业后重新跑通本地环境，修复日期与牧草计算 bug，优化界面，补写本文档  

初版交作业时功能搭得较全，但有几处逻辑问题（日期推进无效、牧草公式单位混用等），已在本地版修复。

---

## 技术实现

> 以下给 **懂技术** 的读者：面试官、同学、或几个月后回来维护的自己。

### 技术栈

| 层级 | 选型 | 说明 |
|------|------|------|
| 后端 | Python 3 + Flask | 路由、业务逻辑、Session 管理 |
| 数据库 | MySQL 8 | 牛群、牲畜、围场持久化 |
| 前端 | Bootstrap 5 + Jinja2 | 模板继承，无自定义 CSS 文件 |
| 部署（原版） | PythonAnywhere | 课程要求；本地版跑在 `127.0.0.1:5000` |

### 架构概览

```
浏览器
  ↓ HTTP
Flask (app.py)
  ↓ SQL
MySQL (fms 库)
  ├── paddocks   围场
  ├── mobs       牛群 → 关联 paddock
  └── stock      牲畜 → 关联 mob

Session (cookie)
  └── curr_date  模拟日期（存在浏览器会话，不依赖真实时钟）
```

### 数据库关系

```
paddocks (围场)
  id, name, area, dm_per_ha, total_dm

mobs (牛群)
  id, name, paddock_id  ──→  paddocks.id
  （paddock_id 唯一：一块地最多一群牛）

stock (牲畜)
  id, mob_id  ──→  mobs.id, dob, weight
```

### 牧草模拟算法

每推进一天，对每个围场：

```
growth       = area × 65           # kg DM/天（生长）
consumption  = stock_num × 14      # kg DM/天（被吃）
total_dm     = total_dm + growth - consumption
dm_per_ha    = total_dm / area
```

**告警阈值（DM/ha）：**

| 颜色 | 条件 |
|------|------|
| 正常 | ≥ 1800 kg DM/ha |
| 黄色 | < 1800 |
| 红色 | < 1500 |

### 主要路由

| 路径 | 作用 |
|------|------|
| `/` | 首页 |
| `/mobs` | 牛群列表 |
| `/stock` | 牲畜明细（按 mob 分组） |
| `/paddocks` | 围场列表 + 推进日期 |
| `/move_mob` | 移动牛群 |
| `/edit_paddocks` | 编辑/新增围场 |
| `/reset-date` | 重置模拟日期 |
| `/reset` | 重置数据库 + 日期 |

### 项目文件

| 文件 | 作用 |
|------|------|
| `app.py` | Flask 主程序 |
| `connect.py` | 数据库配置（本地，勿公开） |
| `fms-local.sql` | 建库 + 初始数据 |
| `import_db.py` | 一键导入数据库 |
| `templates/` | HTML 模板 |
| `requirements.txt` | Python 依赖 |

---

## 本地运行（开发者）

**环境：** Python 3.10+、MySQL 8

```powershell
cd "你的路径\fms-20240924"

pip install -r requirements.txt

copy connect.example.py connect.py
# 编辑 connect.py，填入 MySQL 密码

python import_db.py
python app.py
```

浏览器打开：**http://127.0.0.1:5000/**

**日常启动：** 只需 `python app.py`  
**恢复初始数据：** `python import_db.py` 或访问 `/reset`

---

## 使用说明（页面速查）

| 页面 | 英文菜单 | 干什么 |
|------|----------|--------|
| 首页 | Home | 介绍 + 快捷入口 |
| 牛群 | Mobs | 每群在哪个围场、头数、均重 |
| 牲畜 | Stock | 每头牛的明细，可 Edit |
| 围场 | Paddocks | **核心页**：牧草 + 推进日期 |
| 搬家 | Move mob | 选 mob → 选空围场 |
| 编辑 | Edit paddocks | 改参数 / 新增围场 |

**模拟日期：**

- 本地版起始日 = 启动应用当天
- **Reset date** → 回到起始日
- **Advance to next day** → 日期 +1，牧草重算

---

## 常见问题

**Q：日期为什么不是真实今天？**  
A：这是 **模拟日期**，专门用来推演农场状态。真实日历和模拟日历是两回事。

**Q：Reset date 之后还是旧日期？**  
A：重启 Flask，再点 Reset date；或用无痕窗口打开（清 session）。

**Q：黄行/红行是什么意思？**  
A：这块围场的牧草 DM/ha 偏低或过低，该考虑移牛或补料了（模拟层面）。

**Q：connect.py 能放 GitHub 吗？**  
A：不能，含数据库密码，已在 `.gitignore`。

---

## 后续计划

- [x] 本地跑通 + 修日期 / 牧草 bug
- [x] Bootstrap 界面优化
- [ ] 移动 mob 时只显示空围场
- [ ] 挂到个人站 `ruirui.work` 作展示

---

*最后更新：2026-08-28*
